import {readFileSync,writeFileSync} from 'node:fs';
const data=JSON.parse(readFileSync(new URL('../syllabus/data.json',import.meta.url),'utf8'));
const escape=value=>String(value).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const areas=data.entries.filter(e=>!e.code.includes('.'));
const html=`<!doctype html>
<html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>출제기준 ${data.version} · TOPCIT</title><link rel="stylesheet" href="./style.css"><link rel="stylesheet" href="../shared/site-navigation.css"><script type="module" src="../shared/site-navigation.mjs"></script></head><body><a class="skip" href="#main">본문으로 바로가기</a><main id="main"><header><p class="eyebrow">TOPCIT · ${data.version}</p><h1>출제기준</h1><p>공식 사이트의 출제기준을 옮긴 학습용 복사본입니다. 번호·분류·기준 문장을 원문대로 보관했습니다.</p><p class="source">가져온 날짜: <time>${data.retrieved}</time> · <a href="${escape(data.source)}" target="_blank" rel="noopener">공식 원본 확인 ↗</a><br>이후 개정 내용은 자동으로 반영되지 않습니다.</p></header><nav class="contents" aria-label="평가영역">${areas.map(e=>`<a href="#ref-${e.code}">${e.code}. ${escape(e.text)}</a>`).join('')}</nav><p>4개 평가영역 · 세부 기준 ${data.entries.filter(e=>e.code.split('.').length===4).length}개</p>${data.entries.map(e=>{const depth=e.code.split('.').length;return depth<4?`<h${depth+1} id="ref-${e.code}"><a href="#ref-${e.code}">${e.code}</a> ${escape(e.text)}</h${depth+1}>`:`<article class="criterion" id="ref-${e.code}" data-tts-content><a class="code" href="#ref-${e.code}" aria-label="출제기준 ${e.code}">${e.code}</a><p class="tts-readable">${escape(e.text)}</p></article>`;}).join('\n')}</main></body></html>\n`;
writeFileSync(new URL('../syllabus/index.html',import.meta.url),html);
console.log(`Built syllabus: ${data.entries.length} entries`);
